# /Users/kforsthoevel/.config/tmuxinator/ops.yml

name: ops
root: ~/git/ivx/cloud-ops/main

windows:
  - main:
      root: ~/git/ivx/cloud-ops/main
      layout: even-vertical
      panes:
        - nvim
        -
